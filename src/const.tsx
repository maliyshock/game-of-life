export const RESOLUTION = 10; // or size of cell
export const WIDTH = window.innerWidth
export const HEIGHT = window.innerHeight
export const GAME_SPEED = 500;


export const COLS = Math.floor(WIDTH / RESOLUTION);
export const ROWS = Math.floor(HEIGHT / RESOLUTION);

export const HUMAN_COLOR = '#00D2FF'